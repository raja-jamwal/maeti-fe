import * as React from 'react';
import { View, Text } from 'react-native';

export default class TextProfile extends React.PureComponent {
	render() {
		return (
			<View>
				<Text>Profile</Text>
			</View>
		);
	}
}
